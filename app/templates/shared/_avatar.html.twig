{% if profile and profile.image %}
    <img src="{{ profiles ~ profile.image }}" alt="" class="inline-block h-{{ size }} w-{{ size }} rounded-full ring-2 ring-white">
{% else %}
    <div class="inline-block h-{{ size }} w-{{ size }} bg-gray-200 rounded-full ring-2 ring-white"></div>
{% endif %}